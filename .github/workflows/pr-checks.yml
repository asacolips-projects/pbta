name: PR Checks
on:
  pull_request_target:

concurrency:
  group: "${{ github.ref }}"
  cancel-in-progress: true
jobs:

  #####################################################################
  ### Build Job #######################################################
  #####################################################################
  build:
    if: github.repository_owner == 'asacolips-projects' && contains(github.head_ref, 'actions') == false
    uses: ./.github/workflows/shared-build.yml